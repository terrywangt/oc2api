services:
  oc2api:
    build:
      context: .
      dockerfile: Dockerfile.authfix
    image: oc2api:authfix
    container_name: oc2api
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: always