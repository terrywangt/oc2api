services:
  oc2api:
    build:
      context: .
      dockerfile: Dockerfile.authfix
    image: oc2api:authfix
    container_name: oc2api
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - /opt/oc2api/config.yaml:/app/config.yaml:ro
    restart: always
